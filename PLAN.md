# 專案計畫

本計畫以「先建立可理解、可重現的基線，再進行控制與安全重構」為原則。詳細任務與勾選狀態由 [TASKS.md](TASKS.md) 管理，實際進展見 [PROGRESS.md](PROGRESS.md)。

## 目標

1. 讓 6 套 Keil 工程都能被一致地定位、建置與比較。
2. 保留各輸出版本的控制參數，同時降低重複程式碼造成的漂移。
3. 明確區分即時中斷控制、前台保護與硬體關斷責任。
4. 建立可追蹤的架構、決策、驗證與變更紀錄。

## 階段 0：文件與架構基線

狀態：已完成。

- 建立應用模組 UML、狀態機、控制時序與資料流。
- 建立 C4 System Context、Container、Component 與 Deployment View。
- 建立啟動、前台與 HRTIM ISR 呼叫圖。
- 建立根目錄協作、計畫、進度、決策、任務、記憶、變更及架構文件。
- 記錄六個例程的控制模式、PWM/REP 與主要參數差異。

## 階段 1：可重現建置基線

狀態：待執行。

- 確認每個例程的 Keil target、MCU 型號、編譯器版本與最佳化設定。
- 建置全部 6 套工程，保存 warning/error 摘要及映像大小。
- 建立最小化的建置檢查方式；若無法命令列建置，記錄 GUI 操作與必要工具版本。
- 確認啟動檔、連結設定與實際 STM32G474 型號一致。

## 階段 2：即時資料與安全基線

狀態：待執行。

- 修正並驗證 ISR/前台共享資料的 `volatile` 與一致性策略。
- 統一 `ADC2_RESULT` 宣告尺寸。
- 量測 HRTIM CMP3、ADC 轉換與 DMA 更新的實際相位；確認 `ContinuousConvMode` 是否符合設計意圖。
- 量測 REP ISR 頻率與最壞執行時間。
- 明確定義 OVP/UVP/OCP 的連續計數、累計計數、恢復與鎖存語意。
- 評估 HRTIM fault/COMP 硬體關斷鏈，而不是把軟體保護視為逐周期保護。

## 階段 3：非阻塞狀態機與可測試控制

狀態：待執行。

- 將 1 秒忙等待軟啟動改為非阻塞時間狀態，確保保護持續執行。
- 將 ADC 換算、PI 計算與保護判定分離成可獨立測試函式。
- 為 PI 飽和、雙環最小值選擇、軟啟動斜率及保護滯回建立主機端測試向量。
- 明確定義 `Data_update_flag` 單槽通知是否足夠，或改為序號/雙緩衝快照。

## 階段 4：降低六份重複程式碼

狀態：待決策。

- 比較「保留六個獨立工程」與「共用核心 + 每例程設定檔」兩種方案。
- 若採共用核心，先抽離硬體不變的 ADC/HRTIM/狀態機介面，再移動控制參數。
- 每一步都需保持至少一個單環及一個雙環工程可建置，避免一次性大改。

## 成功條件

- 6 套工程都有可重現建置結果。
- 即時資料共享沒有未說明的編譯器可見性風險。
- PWM、ADC 觸發、REP ISR 及保護延遲都有量測或可重現證據。
- 各例程參數有單一、清楚的來源，且變更能被差異審查。
- 架構圖、呼叫圖、決策、任務與變更紀錄和程式碼保持一致。

## 目前不在範圍內

- 未經硬體規格與測試，不重新調整 PI 參數或保護門檻。
- 不宣稱 12 V 輸入與各目標輸出名稱能代表完整功率級能力；本計畫只記錄目錄與程式碼事實。
- 不因清理目的修改 STM32 HAL/LL、CMSIS 或第三方供應商內容。
