# 任務清單

狀態標記：`[ ]` 待辦、`[~]` 進行中、`[x]` 完成、`[!]` 受阻。優先級依序為 P0、P1、P2。

## 已完成

- [x] DOC-001（P0）分析 6 個例程的目錄與控制模式。
- [x] DOC-002（P0）建立 `docs/UML.md`，包含模組、狀態機、時序與資料流。
- [x] DOC-003（P0）建立 `docs/CALL_GRAPH.md`，包含啟動、前台、ISR 與硬體隱式邊。
- [x] DOC-004（P1）以 Mermaid CLI 渲染驗證全部 7 個圖表。
- [x] DOC-005（P1）建立根目錄專案治理與架構文件。
- [x] DOC-006（P1）建立並渲染驗證 `docs/C4.md`，描述系統上下文、邏輯容器、應用元件與部署。

## 建置基線

- [ ] B-001（P0）列出 6 個 Keil target、裝置、編譯器版本與最佳化設定。
- [ ] B-002（P0）逐一建置 6 套工程並記錄 error、warning、code/RO/RW/ZI 大小。
- [ ] B-003（P0）記錄可重現的建置步驟與工具版本。
- [ ] B-004（P1）確認每套工程選用的 STM32G474 啟動檔與實際硬體一致。

## 即時與安全

- [ ] RT-001（P0）盤點所有 ISR/前台共享變數，提出 `volatile`、臨界區或快照方案。
- [ ] RT-002（P0）統一 `ADC2_RESULT` 的定義與外部宣告尺寸，並建置單環/雙環代表工程。
- [ ] RT-003（P0）驗證 HRTIM CMP3 至 ADC/DMA 的實際取樣相位。
- [ ] RT-004（P0）量測各例程 REP ISR 頻率與最壞執行時間。
- [ ] SAFE-001（P0）定義 OVP/UVP/OCP 的觸發、連續性、滯回、鎖存與恢復需求。
- [ ] SAFE-002（P0）評估 COMP2/DAC/HRTIM fault 硬體關斷鏈。
- [ ] SAFE-003（P0）確認故障關斷延遲與 PA11、TA1/TA2 的實際波形。

## 控制與狀態機

- [ ] CTRL-001（P1）將 1 秒忙等待軟啟動改為非阻塞狀態。
- [ ] CTRL-002（P1）為單環 PI、雙環最小值選擇與輸出限幅建立測試向量。
- [ ] CTRL-003（P1）確認 `Vref/Iref += 0.001` 在不同 REP 頻率下對應的實際斜率。
- [ ] CTRL-004（P1）決定 `Data_update_flag` 是否改為計數器或一致資料快照。

## 維護性

- [ ] MAINT-001（P1）移除 `common.h` 循環 include，改為最小必要 include。
- [ ] MAINT-002（P1）把 ISR 從 `state_machine.c` 移至清楚的中斷/控制排程模組。
- [ ] MAINT-003（P2）確認 `TIM2`、`ERROR_flag`、`PC_command`、`Target_IL/ILref` 等未使用項目的意圖。
- [ ] MAINT-004（P2）評估抽取共用核心及每例程參數設定檔；執行前需新增決策紀錄。

## 任務完成條件

- 程式修改已在適當例程建置，且沒有未說明的新 warning。
- 即時或保護修改列出板測結果；沒有硬體時明確標示未板測。
- 對應文件、決策、進度與變更紀錄已同步。
- `git diff --check` 通過，且差異中沒有意外編碼轉換或供應商檔案修改。
