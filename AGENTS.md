# AGENTS.md

本文件適用於整個 `STM32G474_BUCK` 倉庫，供後續維護者與自動化代理遵循。

## 專案目標

維護 6 套 STM32G474 數位電源範例，在不破壞既有硬體時序、保護條件與 Keil 工程可建置性的前提下，逐步改善可讀性、可驗證性與共用性。

## 語言與格式

- 新增或更新的專案文件使用繁體中文與 UTF-8。
- 函式名、暫存器名、巨集名及原始碼識別字保持原樣，不翻譯。
- 不因文件或小型修正而批次轉換既有 C/H 檔案編碼；部分原始碼註解不是 UTF-8，必須避免無關的整檔重寫。
- Markdown 使用相對連結；圖表優先採用 Mermaid，讓架構變更可由 Git 審查。

## 倉庫結構

- `例程1_*`～`例程3_*`：800 kHz、電壓單閉環，目標輸出依序為 5/12/16 V。
- `例程4_*`～`例程6_*`：200 kHz、恆壓恆流雙 PI，目標輸出依序為 5/12/16 V、3 A。
- 每個例程的 `mycode/`：應用控制、ADC、HRTIM、狀態機、延遲與板級 IO。
- 每個例程的 `User_code/`：`main.c`、系統初始化、例外與 HAL MSP。
- 每個例程的 `HAL_lib/`、`MDK/`：供應商函式庫、CMSIS、啟動檔與 Keil 工程。
- `docs/`：詳細 UML 與呼叫圖。
- 根目錄管理文件：計畫、進度、決策、任務、記憶、變更與架構入口。

## 工作前必讀

依工作類型閱讀最小必要文件：

1. 先讀 [ARCHITECTURE.md](ARCHITECTURE.md) 與 [MEMORY.md](MEMORY.md)。
2. 修改即時控制或保護前，必須讀 [docs/UML.md](docs/UML.md) 與 [docs/CALL_GRAPH.md](docs/CALL_GRAPH.md)。
3. 開始新工作前檢查 [TASKS.md](TASKS.md)、[PLAN.md](PLAN.md) 與 [DECISIONS.md](DECISIONS.md)。
4. 使用 `git status --short` 確認使用者既有變更；不得覆寫或清除不屬於目前任務的修改。

## 原始碼修改規則

- 先明確指出修改哪一個例程及控制模式，不得假設 6 套工程完全相同。
- 例程 1～3 與例程 4～6 的共同骨架相似，但 PI 介面、參數、REP 設定與 OCP 計數語意不同；跨例程同步修改前必須逐一比較。
- 未經明確要求，不要直接修改 `HAL_lib/`、CMSIS 或啟動檔。
- 不得擅自改變 `PWM_PERIOD`、REP、PI 增益、占空比限制、OVP/UVP/OCP 門檻、ADC 換算係數、死區或輸出極性。
- 保持安全啟動順序：先讓 ADC/DMA 就緒，再啟動 HRTIM；前台 `Reset_VAR()` 必須關閉 PA11 驅動及 HRTIM TA1/TA2。
- 保持故障關斷的雙重門控：`DISdriver` 與 HRTIM `ODISR` 不可只保留其中一層，除非有硬體驗證與明確決策紀錄。
- ISR 內避免阻塞、動態配置、日誌輸出及不受限迴圈。任何新增計算都要評估最壞執行時間。
- 跨 ISR/前台共享資料必須明確處理 `volatile`、原子性與一致快照；不要只用註解假設同步成立。
- 不要宣稱通過硬體驗證，除非實際記錄示波器、邏輯分析儀或板上測試結果。

## 文件同步規則

完成工作時依影響更新：

- 架構、狀態機、即時資料流改變：更新 `ARCHITECTURE.md`、`docs/UML.md`、`docs/CALL_GRAPH.md`。
- 做出會約束後續實作的選擇：追加 `DECISIONS.md`，不要覆寫歷史決策。
- 任務狀態改變：更新 `TASKS.md` 與 `PROGRESS.md`。
- 新增長期有效的專案事實或陷阱：更新 `MEMORY.md`。
- 使用者可見變更：更新 `CHANGELOG.md` 的 `Unreleased`。

## 驗證要求

依修改風險執行足夠驗證：

1. 文件：檢查相對連結、程式碼圍欄、尾端空白與 Mermaid 渲染。
2. 單一例程 C/H 修改：至少建置該 Keil target，並確認無新增 warning。
3. 共用模式修改：分別建置一個單環例程及一個雙環例程；若同步到 6 套工程，建置全部 6 套。
4. PWM、ADC 觸發、PI 或保護修改：除編譯外，列出需要的板上量測；未實測時在 `PROGRESS.md` 明確標示。
5. 交付前執行 `git diff --check`，並審查 `git diff` 是否包含意外的編碼或供應商檔案變更。

## 完成定義

只有在程式/文件、適當驗證、任務狀態與必要決策紀錄一致時，工作才算完成。若缺少 Keil、硬體或量測設備，應標記為「已完成靜態分析／尚待建置或板測」，不可把限制隱藏在結論中。
